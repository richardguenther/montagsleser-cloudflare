---
title: versuch
eleventyNavigation:
  key: versuch
  order: 3
---
{% extends "base.njk" %}

{% block main %}
  <h1 class="mx-auto my-2 text-6xl text-red-900">Montagsleser</h1>

  {% for post in wp %}

    {{wp[0].title.rendered | dump}}

    <p>https://acf.montagsleser.de/wp-content/uploads/{{wp[0]['_embedded']['wp:featuredmedia'][0]['media_details']['file']}}</p>

    {% image "https://acf.montagsleser.de/wp-content/uploads/{{wp[0]['_embedded']['wp:featuredmedia'][0]['media_details']['file']}}", "photo of my cat", "(min-width: 30em) 50vw, 100vw" %}

  {% endfor %}

{% endblock %}

{# const imageShortcode = async (
  relativeSrc,
  alt,
  className,
  widths = [null, 300, 800, 1280],
  formats = ['webp'],
  sizes = '(min-width: 100px)' #}